gateway:
  image:
    tag: prod
  api:
    ingress:
      enabled: true
      host: verify.example.com
      tls:
        enabled: true
        secretName: verify-prod-tls
  env:
    HTTP_ALLOWED_ORIGINS: https://web.example.com
    OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.monitoring.svc.cluster.local:4317
registry:
  image:
    tag: prod
  ingress:
    enabled: true
    host: registry.example.com
    tls:
      enabled: true
      secretName: registry-prod-tls
  env:
    OIDC_ISSUER_URI: https://auth.example.com/realms/diasoft
    APP_ALLOWED_ORIGINS: https://web.example.com
    OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.monitoring.svc.cluster.local:4317
web:
  image:
    tag: prod
  ingress:
    enabled: true
    host: web.example.com
    tls:
      enabled: true
      secretName: web-prod-tls
  runtimeConfig:
    VITE_AUTH_ENABLED: "true"
    VITE_OIDC_AUTHORITY: https://auth.example.com/realms/diasoft
    VITE_OIDC_CLIENT_ID: diasoft-web
    VITE_OIDC_REDIRECT_URI: https://web.example.com/
    VITE_OIDC_POST_LOGOUT_REDIRECT_URI: https://web.example.com/
    VITE_GATEWAY_BASE_URL: https://verify.example.com
    VITE_REGISTRY_BASE_URL: https://registry.example.com
keycloak:
  ingress:
    enabled: true
    host: auth.example.com
    tls:
      enabled: true
      secretName: auth-prod-tls
  admin:
    existingSecret: keycloak-admin
  realm:
    redirectUris:
      - https://web.example.com/*
    postLogoutRedirectUris:
      - https://web.example.com/*
