apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: diasoft-web-stage
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: https://gitlab.example.com/platform-infra.git
    targetRevision: main
    path: helm/charts/diasoft-web
    helm:
      valueFiles:
        - ../../environments/stage/diasoft-web.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: diasoft-stage
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
