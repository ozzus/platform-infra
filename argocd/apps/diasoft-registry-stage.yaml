apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: diasoft-registry-stage
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: https://gitlab.com/ozzus/platform-infra.git
    targetRevision: main
    path: helm/charts/diasoft-registry
    helm:
      valueFiles:
        - ../../environments/stage/diasoft-registry.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: diasoft-stage
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
