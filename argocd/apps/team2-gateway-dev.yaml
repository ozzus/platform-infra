apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team2-gateway-dev
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  project: team2
  source:
    repoURL: https://github.com/ozzus/platform-infra.git
    targetRevision: main
    path: helm/charts/diasoft-gateway
    helm:
      valueFiles:
        - ../../tenants/team2/dev/diasoft-gateway.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: team2-dev-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
