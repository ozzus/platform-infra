apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team1-keycloak-dev
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: team1
  source:
    repoURL: https://gitlab.com/ozzus/platform-infra.git
    targetRevision: main
    path: helm/charts/keycloak
    helm:
      valueFiles:
        - ../../tenants/team1/dev/keycloak.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: team1-dev-auth
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
