apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: diasoft-gateway-dev
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: https://gitlab.com/ozzus/platform-infra.git
    targetRevision: main
    path: helm/charts/diasoft-gateway
    helm:
      valueFiles:
        - ../../environments/dev/diasoft-gateway.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: diasoft-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
